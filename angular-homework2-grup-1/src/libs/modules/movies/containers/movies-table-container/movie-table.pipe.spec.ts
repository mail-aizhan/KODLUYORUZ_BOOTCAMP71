import { MovieTablePipe } from './movie-table.pipe';

describe('MovieTablePipe', () => {
  it('create an instance', () => {
    const pipe = new MovieTablePipe();
    expect(pipe).toBeTruthy();
  });
});
